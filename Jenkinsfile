pipeline {
    agent any
    
    stages {
        stage('Python_build') {
            steps {
                sh "whoami"
     sh "python --version"
               //sh "echo Hello from the shell"
         }
        }
    }
}
