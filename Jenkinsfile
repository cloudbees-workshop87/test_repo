pipeline {
    agent any
    
    stages {
        stage('Python_build') {
            steps {
                sh 'whoami'
        sh 'python abc.py'
                sh "echo Hello from the shell"
         }
        }
    }
}
